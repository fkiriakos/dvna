version: 2.1
orbs:
  shiftleft: shiftleft/shiftleft@1.0.6

jobs:
  shiftleft:
    machine: true
    steps:
      - checkout
      - shiftleft/analyze:
          app: ${CIRCLE_PROJECT_REPONAME}
          target: ${CIRCLE_PROJECT_REPONAME}
          language: js
          cpg: true

workflows:
  version: 2.1
  run-sast-scan:
    jobs:
      - shiftleft
