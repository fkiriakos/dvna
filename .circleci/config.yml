version: 2.1
orbs:
  shiftleft: shiftleft/shiftleft@1.0.6

jobs:
  shiftleft:
    machine: true
    steps:
      - checkout
      - shiftleft/analyze:
          app: dvna
          target: ${CIRCLE_WORKING_DIRECTORY}/${CIRCLE_PROJECT_REPONAME}
          language: js
          cpg: true

workflows:
  version: 2.1
  run-sast-scan:
    jobs:
      - shiftleft
